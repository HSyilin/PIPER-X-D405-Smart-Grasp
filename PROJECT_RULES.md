# 项目开发规则

生效日期：`2026-07-26`

本文件是 PIPER-X D405 Smart Grasp 项目的长期开发约束。除非项目所有者
明确修改规则，否则后续开发、调试、迁移和交付均须遵守。

## 规则一：禁止擅自推送官方上游仓库

1. 默认只允许在本机检查、修改、构建、测试、提交和创建本地标签。
2. 不得向 AgileX、RealSense、MoveIt 或其他第三方官方仓库执行 `git push`。
3. 任何远程推送前，必须先向项目所有者说明：
   - 目标远程仓库和所有者；
   - 目标分支；
   - 将要推送的提交和标签；
   - 是否可能影响官方或公共项目。
4. 只有获得针对该次推送的明确审批后才能执行。普通的“继续开发”或“完成
   功能”不等于授权推送。
5. 项目自有私有仓库以后如需建立，应使用独立 remote，不得把官方 `origin`
   当作项目交付仓库。

## 规则二：所有修改必须可追溯、可回档

1. 源码、配置、接口和文档的交付修改必须进入 Git 提交；不得只保留在未跟踪
   文件或构建目录中。
2. 每个可交付版本至少应更新：
   - `CHANGELOG.md`：说明增加、修改、安全行为和兼容性变化；
   - `RELEASE_MANIFEST.md`：记录跨仓库提交、上游基点、验证结果和排除项；
   - 版本标签：为抓取工作区和所依赖的本地集成建立对应标签。
3. 不得覆盖、回退或提交无法确认归属的既有本地修改。
4. 回档优先从标签创建新分支，或使用 `git revert`；未经明确审批不得使用
   `git reset --hard`、强制推送等破坏历史的操作。
5. 模型、录制数据和大文件不进入源码 Git 时，必须另存并记录版本、来源和
   SHA-256，保证它们能与对应源码版本重新组合。

## 规则三：优先新增，最小侵入既有源码

### 默认实施顺序

对已有工作区，特别是官方驱动、SDK、标定和 MoveIt 包，按以下顺序选择方案：

1. 新建独立 overlay 包、节点、插件、适配器、launch 包装器或配置文件。
2. 使用已有参数、接口、继承点和 ROS 话题/动作完成集成。
3. 如需接入既有入口，只增加最小连接代码，并保持原默认行为和公共接口不变。
4. 只有前三种方法无法实现目标时，才考虑修改既有核心逻辑、默认行为或公共
   接口；此类修改必须先审批，未获审批不得实施。

“更方便”“代码更短”或“以后可能用到”不构成修改既有逻辑的充分理由。

### 可以直接进行的工作

在用户已授权的任务范围内，通常可直接进行：

- 只读检查、构建、测试和诊断；
- 在项目自有工作区新增独立文件、包、测试和文档；
- 新增默认关闭的可选功能；
- 新增不改变既有默认行为的参数或启动包装层；
- 为新增代码修复明确缺陷，并同步增加回归测试。

即使无需单独审批，也必须遵守规则二并保留可回档记录。

### 必须事先审批的源码修改

出现以下任一情况，必须暂停实施并请求项目所有者明确审批：

- 删除、替换或重写已有业务/控制逻辑；
- 改变官方包或既有节点的默认行为；
- 修改已有 ROS Topic、Service、Action、TF、参数或消息的兼容语义；
- 修改硬件使能、速度、力、故障处理或安全门逻辑；
- 为实现新功能必须侵入官方驱动、SDK 或第三方源码；
- 修改会影响现有启动方式、其他设备或既有使用者；
- 无法通过新增模块、参数化或包装层实现目标。

### 审批请求必须包含的内容

请求审批时必须先给出：

1. 当前目标和阻塞原因；
2. 为什么新增模块、适配器或配置无法解决；
3. 准备修改的仓库、文件和具体逻辑；
4. 对默认行为、接口、安全性和兼容性的影响；
5. 计划增加或执行的测试；
6. 对应提交方案和无破坏回档方法。

审批只覆盖说明中的范围。实施过程中若范围扩大，必须重新申请审批。

## 当前基线说明

- `smart-grasp-v0.2.0` 是抓取工作区的首个可复现基线。
- `smart-grasp-integration-v0.2.0` 是官方源码本地克隆中的最小 MoveIt 集成。
- 上述本地集成在本规则写入前已经完成，没有推送到官方仓库，并已单独提交、
  标记和记录回档方法。
- 从本规则生效后，若需要进一步修改该官方源码集成或其他既有核心逻辑，必须
  按规则三先申请审批。

## 执行原则

发生歧义时采用更保守的解释：保持原逻辑、停止侵入性修改、提供方案和影响
说明，等待项目所有者决定。口头或聊天中的审批结果应在对应提交说明、变更日志
或发布清单中留下可查记录。
